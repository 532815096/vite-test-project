.ds-manage-container {
  margin: 20px;
  background-color: #fff;
  box-shadow: 0 0 8px 0 rgba(0, 0, 0, 0.10);
  border-radius: 2px;
  min-height: 400px;
  min-height: calc(100vh - 90px);
  display: flex;
}
